""import React from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { motion } from "framer-motion";

const HomePage = () => {
  return (
    <div className="bg-gradient-to-r from-blue-800 to-blue-500 min-h-screen p-10">
      <motion.div
        className="text-center text-white mb-10"
        initial={{ opacity: 0, y: -50 }}
        animate={{ opacity: 1, y: 0 }}
        transition={{ duration: 0.5 }}
      >
        <h1 className="text-5xl font-bold mb-4">NextGen Analytics & BI (NABI)</h1>
        <p className="text-xl">Llevando la inteligencia de negocios al siguiente nivel</p>
      </motion.div>

      <div className="grid grid-cols-1 md:grid-cols-3 gap-6">
        <Card className="bg-blue-700 text-white">
          <CardContent>
            <h2 className="text-2xl font-semibold mb-2">Análisis de Datos</h2>
            <p>Transforma datos en decisiones estratégicas con nuestras soluciones BI.</p>
            <Button className="mt-4 bg-blue-600 hover:bg-blue-500">Ver Más</Button>
          </CardContent>
        </Card>

        <Card className="bg-blue-700 text-white">
          <CardContent>
            <h2 className="text-2xl font-semibold mb-2">Automatización de Reportes</h2>
            <p>Optimiza procesos y ahorra tiempo con nuestras soluciones automatizadas.</p>
            <Button className="mt-4 bg-blue-600 hover:bg-blue-500">Ver Más</Button>
          </CardContent>
        </Card>

        <Card className="bg-blue-700 text-white">
          <CardContent>
            <h2 className="text-2xl font-semibold mb-2">Integración SAP & SQL</h2>
            <p>Conecta tus datos empresariales y maximiza su valor en cada decisión.</p>
            <Button className="mt-4 bg-blue-600 hover:bg-blue-500">Ver Más</Button>
          </CardContent>
        </Card>
      </div>
    </div>
  );
};

export default HomePage;
""
